{
  "name": "cogsmith",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check sw.js && node --check scripts/capture-visual-review.mjs && node --check scripts/smoke-test.mjs",
    "test": "node scripts/smoke-test.mjs",
    "visual-review": "node scripts/capture-visual-review.mjs"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
